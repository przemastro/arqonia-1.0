<img ng-src="{{mainImageUrl}}" class="object">

<h1>{{object.name}}</h1>

<p>{{object.description}}</p>

<ul class="object-thumbs">
  <li ng-repeat="img in object.images">
    <img ng-src="{{img}}" ng-click=setImage(img)">
  </li>
</ul>
